<h1>Result</h1>

<% if @in_grid %>
  <% if @valid_word %>
    <p><strong>Congratulations!</strong> <%= @guess %> is a valid English word!</p>
  <% else %>
    <p>Sorry but <strong><%= @guess %></strong> does not seem to be a valid English word...</p>
  <% end %>
<% else %>
  <p>Sorry but <strong><%= @guess %></strong> can't be built out of <%= @grid.join(", ") %></p>
<% end %>

<%= link_to "Play again", new_path %>
